$ (document).ready(
  $(window).bind('scroll', function() {
    if ($(window).scrollTop() < 100) {
      $('#nav').removeClass('active');
    }
    else {
      $('#nav').addClass('active');
    }
  });
);
